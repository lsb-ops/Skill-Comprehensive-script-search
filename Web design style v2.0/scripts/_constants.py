"""
WebPPT Maker · TDD 验收阈值常量 (Python 版本)
单一来源: tests/TDD验证方案.md
用途: generate_html.py / generate_copy.py / generate_script.py / generate_subtitle.py 共享阈值
"""

# ============================================================
# A 类 — HTML 网页检查
# ============================================================
# A0: index.html 大小 (硬约束 50KB)
A0_MAX_HTML_BYTES = 51200

# A2: 页数 (≥ 5 页)
A2_MIN_PAGE_COUNT = 5

# A4: 字号 (≥ 24px)
A4_MIN_FONT_PX = 24

# ============================================================
# B 类 — 抖音文案检查 (v1.2.0: 严格化)
# ============================================================
# B1: 标题 (硬约束 ≤ 22 字, 阈值严格 22)
B1_MAX_TITLE_LEN = 22
B1_HARD_TITLE_LEN = 22  # 实际硬截断到 22 字

# B2: 正文 (硬约束 80-150 字, 阈值严格 80-150)
B2_MIN_BODY_LEN = 80
B2_MAX_BODY_LEN = 150  # 触发截断
B2_HARD_BODY_LEN = 140  # 截断到 140 字 (留 buffer 给 CTA)

# ============================================================
# G 类 — 剪映字幕检查
# ============================================================
# G5: 要点数 (≤ 8 个)
G5_MAX_CONTENT_POINTS = 8

# G6: 单段时长 (≤ 5s)
G6_MAX_SEGMENT_DUR = 5.0

# G7: 单段字符数 (≤ 30 字)
G7_MAX_CHARS = 30

# G10: 钩子段 (3s 固定)
G10_HOOK_DUR = 3.0

# G11: CTA 段 (2s 固定)
G11_CTA_DUR = 2.0

# ============================================================
# v3.3: CTA 主题默认值 (单一来源 — 4 个产物都从这里取)
# ============================================================
DEFAULT_CTA_BY_STYLE = {
    "现代简约": "关注我，看更多干货 ✨",
    "知识科普": "关注我，每天学一点 📚",
    "默认": "关注我，看更多精彩内容 ✨",
}
